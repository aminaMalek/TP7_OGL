pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                // Lancement des tests unitaires.
                bat "./gradlew build"
            }
        }
    }
}
